# ナゾテン！ ～なぞって・たして・10の倍数～

1人用スコアアタックは、2×3盤面でお題を解く「おてがる」と、
従来の4×4盤面でフィーバーを狙う「スタンダード」を遊べます。
ほかに、強さ1～5・MAXのCPUと競う「CPUバトル」、
1台の端末を2人で挟んで対戦する「2人バトル」も遊べます。
CPUバトル・2人バトルはどちらも、別々の盤面で得点を競う「スコアバトル」と、
1つの盤面から数字を奪い合う「ごちゃまぜバトル」を選べます。

スタンダードスコアアタックでは、縦4×横4の数字を素早くなぞり、
合計10・20・30・40を作って得点します。ラスト10秒は「ミリオン・フィーバー」が発動し、
獲得得点が3倍になります。また、5マスで合計10を作ると「シルバー・フィーバー」が発動し、
10秒間得点が2倍になります。

時間制限なしで固定盤面のお題を解く、1人用「じっくり」モードも遊べます。
消した数字は補充されないため、数手先まで考えてステージクリアを目指します。

本リポジトリは、スコアアタック（おてがる／スタンダード）、CPUバトル、1台2人対戦に加え、
1人用ステージパズル「じっくり」を追加するPhase 6まで実装したものです。
タイトル画面にハイスコアは表示せず、スコアアタックのモード選択画面で
おてがる・スタンダードそれぞれのBESTを確認できます。

## おてがるスコアアタック

2×3の6マスに表示された数字を使い、問題ごとに指定された合計とマス数を作ります。

- 制限時間は60秒です。
- 全10パターンを10問単位で1問ずつ出題します。
- 初期盤面には、入れかえや破壊を使わない正解が必ずあります。
- 正解すると6つの数字すべてが次の問題へ変わります（切り替え時は各パネルが
  縮小してから、新しいパネルが拡大して出現します）。
- 数字の入れかえを使用できます。
- パネルをダブルタップすると、その問題を0点でパスします。
- ミスなし・入れかえなしで正解すると、1問につき+20点のボーナスが付きます。
- シルバー・フィーバーとミリオン・フィーバーは発動しません。
- 結果画面には、正解数と連続正解数から算出したLV・称号を表示します
  （LV＝正解数＋√連続正解数、小数点以下切り捨て）。
- ハイスコアはスタンダードと別々に保存します。

## スタンダードスコアアタック

1. スコアアタック選択画面で「スタンダード」を押してスタート。
2. 盤面の数字を上下左右につながる形で2〜5個なぞって、合計を10・20・30・40にする。
3. 40を作るとフォーティボーナスで+100点（フィーバー倍率の影響を受けない）。
4. 制限時間は60秒。ラスト10秒は「ミリオン・フィーバー」で得点が3倍になる。
5. 5マスで合計10を作ると「シルバー・フィーバー」が発動し、10秒間盤面が銀色になって得点が2倍になる
   （ミリオン・フィーバー中は発動せず、両方の条件を同時に満たした場合はミリオン・フィーバーが優先）。
6. 邪魔な数字は、素早く2回連続でタップ（ダブルタップ）すると得点なしで破壊できる。
   破壊した位置には1秒後に新しい数字が補充される。
7. 数字を短くタップすると選択状態になり、続けて別の数字をタップするとその2つの位置を
   入れかえられる。選択は、他の数字を長押ししたり、なぞり操作を始めたりすると解除される。

## CPUバトル

「強さ1」から「強さ5」、さらに最強の「MAX」まで、6段階のCPUと60秒間のスコア対決ができます。
「CPUバトル」を押すと、まず対戦形式（スコアバトル／ごちゃまぜバトル）とオジャマのON／OFF
（初期値はON）を選んでから、CPUの強さをスライドバーで選びます。

### CPUキャラクター演出

強さ1～5・MAXの6段階には、それぞれ個性を持つ敵キャラクターが割り当てられています。

| 強さ | 絵文字 | 名前 |
| --- | :---: | --- |
| 1 | 🐥 | ナゾピヨ |
| 2 | 🐺 | テンウルフ |
| 3 | 🥷 | ナゾリシノビ |
| 4 | 🧙 | ひらめきメイジ |
| 5 | 🐉 | フォーティドラゴン |
| MAX | 🤖 | NAZOTEN-X |

- 強さ選択画面では、スライダーを動かすたびに絵文字・名前・フレーバーテキスト・CPU性能説明・
  選択中の対戦形式に対応する通算勝利数をまとめたキャラクターカードが更新されます。
- スコアバトルCPU戦・ごちゃまぜバトルCPU戦のどちらも、CPU盤面の右側に絵文字と名前を表示します。
- CPUが優勢なほど絵文字が大きく、劣勢なほど小さく表示され、盤面の位置や操作領域は変わりません。
- ラスト10秒のミリオン・フィーバー中は絵文字が等倍のまま固定され、戦況を推測できなくなります。
- タイムアップ後の結果画面では、CPU視点の勝敗・点差を7段階（大勝・勝利・僅差勝ち・引き分け・
  僅差負け・敗北・大敗）に分類し、キャラクターごとに用意されたセリフを吹き出しで表示します。
- キャラクター演出は表示のみの機能で、CPUの思考速度・探索精度・得点・戦績には一切影響しません。

### スコアバトル

- プレイヤーとCPUは同じ数字の並びからスタートします。
- スマホ縦画面で1画面に収まるよう、両者の盤面は通常サイズの80%で表示されます。
- 開始後の消去・補充はそれぞれ独立しています（片方の操作がもう一方の盤面に影響しません）。
- プレイ中、正確なスコアは表示されません。
- 中央のバトルゲージ（点差を基準に正規化した割合）で、どちらが優勢かを確認できます。
- ラスト10秒はゲージが消え、両者の得点がミリオン・フィーバーで3倍になります。
- シルバー・フィーバー（5マスで合計10）はプレイヤー・CPUそれぞれ独立して発動し、発動した側の盤面だけが
  10秒間銀色になって得点2倍になります（ミリオン・フィーバー中は発動せず、優先度もミリオン・フィーバーが上）。
- タイムアップ後に両者のスコアと勝敗が発表されます。
- CPUは盤面を探索して経路を選び、実際になぞる演出をしたうえで得点を確定します。強さに応じて
  思考の速さ・見つけられる経路の長さ・最善手を選ぶ精度が変わり、わざと失敗したり、手詰まり時に
  得点なしで数字を破壊したりすることもあります。
- CPUレベルごとに、通算成績（勝敗数・自己最高得点など）を保存します。

### ごちゃまぜバトルCPU戦

- 1つの盤面をプレイヤーとCPUが共有し、同じマス・経路を早い者勝ちで奪い合います
  （仕組みは2人バトルのごちゃまぜバトルと共通）。
- 先に確定した側が得点・消去を獲得し、後から確定した側はペナルティなしで無効化されます。
- CPUが現在なぞっている最中のマスは、プレイヤーが入れかえ・破壊できません。
- 結果画面では称号・LVを表示しません（スコアバトルCPU戦との違い）。
- CPUレベルごとに、スコアバトルCPU戦とは別枠で通算成績を保存します。

### オジャマ（CPUバトル共通）

- オジャマをONにすると、CPUバトルでもスコアバトル・ごちゃまぜバトルの両方でオジャマが有効になります。
  経過20秒・40秒（残り40秒・20秒）で自動発動する仕組み・5種類の視覚効果（ターン・スモール・
  スマイル・メテオ・ハイド）は2人バトルのオジャマと共通です。
- CPUが勝っている側になった場合は、CPU自身が自動的にオジャマの対象になり、見た目の種類に
  かかわらず7秒間CPUの思考時間・なぞり操作時間が2倍になり、CPUの動きが遅くなります。

## 2人バトル

1台のスマートフォンやタブレットを2人で挟み、60秒間のスコアを競います。
「2人バトル」を押すと対戦形式（スコアバトル／ごちゃまぜバトル）とオジャマのON／OFFを
選ぶ画面が開きます。この設定はページ内でのみ記憶され、再読み込みすると
スコアバトル・オジャマONへ戻ります。

### スコアバトル

Phase 4から続く、それぞれ独立した盤面で得点を競う形式です。

- 画面下が青の1P、画面上が赤の2Pです。
- 2P側の盤面と表示は180度回転します（盤面のセル配置自体はP1・P2で共通で、CSSの回転
  だけで2P側から見て正しい向きになります）。
- 両者は同じ4×4の数字配置からスタートします。
- 開始後の消去・補充・入れかえはそれぞれ独立しています（片方の操作がもう一方の盤面に
  影響しません）。
- 2人が同時になぞり、破壊、数字入れかえを操作できます。Pointer
  Eventの入力コントローラーをP1・P2で別インスタンスにすることで、一方の指の動きが
  相手の盤面へ影響しません。
- プレイ中は正確な得点を表示せず、中央ゲージ（点差を基準に正規化した割合）で優勢を
  示します。
- シルバー・フィーバー（5マスで合計10）はプレイヤーごとに独立して発動し、発動した側の
  盤面だけが10秒間銀色になって得点2倍になります。
- シルバー・フィーバー中に再び5マスで10を作ると、倍率は2倍のまま重複させず、残り時間が
  10秒へ更新されます。
- ラスト10秒はゲージが消え、両者ともミリオン・フィーバーで得点3倍になります。ミリオン・
  フィーバー開始時、発動中のシルバー・フィーバーは両者とも即座に終了します。
- タイムアップ後に両者の得点・称号・勝敗・プレイ記録を、それぞれの向きから確認できる
  結果画面で発表します。
- 残り時間・SILVERバッジ・「もどる」ボタンは各プレイヤーの盤面の左側（2P側は反転しているため
  見た目上は右側）にまとめて表示し、盤面の位置がずれないようにしています。対戦中は
  「リトライ」を置かず、「もどる」でいつでもタイトルへ戻れます。
- 中央の優劣ゲージは「1P（バー）2P」の1行にまとめて表示し、2Pの文字だけ180度回転させています。

### ごちゃまぜバトル

P1・P2が同じ4×4盤面を共有し、数字を早い者勝ちで奪い合います。

- 相手がなぞっている数字は相手色（P1＝青、P2＝赤）でリアルタイム表示されます。同じマスを
  両者が選んでいる場合は、自分の選択（濃い塗り）と相手の選択（色つきリング）が重なって見えます。
- 同じ数字を2人が同時になぞることもできます。
- 先に正解を完成させた側が得点し、数字を共有盤面から消します（両方の盤面表示へ同時に反映）。
- 相手に先に数字を消された経路は、失敗扱いになりません（結果画面の「先に取られた回数」に記録）。
- 相手が現在なぞっている・入れかえ用に選んでいる数字は、入れかえ・破壊できません
  （確定の直前に再検証するため、選択タイミングが競合しても盤面は不整合になりません）。
- 消去・補充・入れかえ・破壊は両者の画面へ同期されます。
- シルバー・フィーバーは各プレイヤー別に発動し、ミリオン・フィーバーは両者共通です。
- 結果画面では得点・勝敗・プレイ記録を表示し、称号（LV表示）は表示しません。

## じっくりモード

制限時間なしで、固定された数字の盤面とお題に挑戦します。

- 3×3、4×4、5×5の全24ステージです。
- 数字を消しても新しい数字は補充されません。
- 指定合計、指定マス数、順番、全消去のお題があります。
- 一部ステージでは数字を入れかえられ、入れかえも1手として数えます。
- ダブルタップ破壊、得点、フィーバー、称号は使用しません。
- 困ったときは「1手戻す」「ヒント」「やり直す」を使えます。
- クリア、目標手数、ノーヒントの条件で最大3スターを獲得できます。
- 各エリアのStage 6をクリアすると「ランダム問題」が解放されます。
- ランダム問題は、正解手順から逆算して作られるため、必ず解法があります。
- ランダム問題の盤面は3×3または4×4で、5×5は出題されません。
- エリアごとに、指定合計・順番・全消去・入れかえ問題が出題されます。
- 同じ問題への再挑戦と、新しいランダム問題への連続挑戦ができます。
- ランダム問題は固定ステージのスター合計には含まれません。

## オジャマ

2人バトル（スコアバトル・ごちゃまぜバトルの両方）では、対戦前にオジャマのON／OFFを選べます
（初期値はON）。

- ボタン操作はありません。経過20秒・40秒（＝残り40秒・20秒）の時点で、その瞬間に
  勝っている側が自動的にオジャマの対象になります（同点の場合は発動しません）。1試合につき
  最大2回です。
- 対象になると7秒間、ランダムな視覚的オジャマ（ターン・スモール・スマイル・メテオ・ハイドの
  5種類から抽選）が発生します。
- ターン：対象プレイヤー側の数字だけを180度回転させます（盤面・当たり判定は変えません）。
- スモール：対象プレイヤー側の数字をマスごとにランダムな大きさへ縮小します（セルの大きさ・
  タッチ領域は変えません）。
- スマイル：対象プレイヤー側の盤面上を大きな絵文字が動き、数字の一部を隠します
  （`pointer-events: none`のためタッチ操作は妨げません）。
- メテオ：対象プレイヤー側の盤面の右上から左下へ複数の⭐が降り注ぎ、数字の一部を隠します
  （こちらも`pointer-events: none`でタッチ操作は妨げません）。
- ハイド：対象プレイヤー側の数式表示（「3+3+4=10」など）を黒い矩形で完全に隠します。
- オジャマを受けた側には、残り時間表示のすぐ下に「オジャマ「メテオ」」のように受けた種類を
  7秒間表示します。
- オジャマは表示だけを変え、数字・座標・操作判定には一切影響しません。
- ラスト10秒のミリオン・フィーバーが始まると、進行中のオジャマは即座に解除されます
  （重複しません）。

## 得点計算

```text
得点 = 合計 × なぞったマス数 × フィーバー倍率 ＋ フォーティボーナス
```

- フォーティボーナス：合計が40のときだけ+100点。フィーバー倍率の影響を受けない。
- フィーバー倍率：「ミリオン・フィーバー」中は3倍、「シルバー・フィーバー」中は2倍、それ以外は1倍。
- 倍率確定のタイミングは、指を離して成功が確定した瞬間のフェーズ。
- シルバー・フィーバー（5マスで合計10）は、ミリオン・フィーバー中でないときにだけ発動し、
  10秒間得点が2倍になる。発動のトリガーとなった手自体は発動前の判定のため通常倍率で加点される。
  ミリオン・フィーバーとは同時発動せず、両方の条件を満たした場合はミリオン・フィーバーが優先される。

| 条件 | 計算式 | 得点 |
| --- | --- | ---: |
| 通常時に3→7（合計10・2マス） | 10 × 2 + 0 | 20点 |
| ミリオン・フィーバー中に3→7 | 10 × 2 × 3 + 0 | 60点 |
| シルバー・フィーバー中に3→7 | 10 × 2 × 2 + 0 | 40点 |
| 通常時に4マスで合計30 | 30 × 4 + 0 | 120点 |
| ミリオン・フィーバー中に4マスで合計30 | 30 × 4 × 3 + 0 | 360点 |
| 通常時に5マスで合計40 | 40 × 5 + 100 | 300点 |
| ミリオン・フィーバー中に5マスで合計40 | 40 × 5 × 3 + 100 | 700点 |

## 称号一覧

最終スコアに応じて、結果画面に「LV.N　称号名」の形で称号が表示されます。レベルは

```text
LV = floor(√(スコア ÷ 10))　※ 最低LV.1、上限なし
```

で決まります。LV.1〜LV.20は固有の称号名を持ちますが、LV.21以降も上限なく続き、
「超速ナゾテン王+N」（N = レベル − 20）と表示されます。
（例：6,250点はLV.25になるので「LV.25　超速ナゾテン王+5」）

| LV | 称号 | 到達スコア |
| ---: | --- | ---: |
| 1 | はじめてのナゾテン！ | 0〜39 |
| 2 | なぞりのたまご | 40〜89 |
| 3 | たし算みならい | 90〜159 |
| 4 | 10づくりチャレンジャー | 160〜249 |
| 5 | ひらめきルーキー | 250〜359 |
| 6 | なぞりファイター | 360〜489 |
| 7 | たし算ハンター | 490〜639 |
| 8 | ひらめきエース | 640〜809 |
| 9 | 10の倍数ハンター | 810〜999 |
| 10 | なぞりテクニシャン | 1,000〜1,209 |
| 11 | フォーティハンター | 1,210〜1,439 |
| 12 | 10の倍数エース | 1,440〜1,689 |
| 13 | なぞりの達人 | 1,690〜1,959 |
| 14 | ひらめき名人 | 1,960〜2,249 |
| 15 | フォーティマスター | 2,250〜2,559 |
| 16 | 超速ナゾリスト | 2,560〜2,889 |
| 17 | ナゾテンマスター | 2,890〜3,239 |
| 18 | ナゾテンチャンピオン | 3,240〜3,609 |
| 19 | 伝説のナゾリスト | 3,610〜3,999 |
| 20 | 超速ナゾテン王 | 4,000〜4,409 |
| 21 | 超速ナゾテン王+1 | 4,410〜4,839 |
| 22 | 超速ナゾテン王+2 | 4,840〜5,289 |
| 23 | 超速ナゾテン王+3 | 5,290〜5,759 |
| 24 | 超速ナゾテン王+4 | 5,760〜6,249 |
| 25 | 超速ナゾテン王+5 | 6,250〜6,759 |
| … | 超速ナゾテン王+N | 以降も同じ計算式で上限なく続く |

称号名は`js/config.js`の`CONFIG.titleLevels`（LV.1〜LV.20の順の配列）で一元管理しています。
LV.21以降は`js/scoring.js`の`getTitleForScore()`が配列末尾（超速ナゾテン王）に`+N`を付けて生成します。

## サウンド

タイトル画面の「◀ [モード名] ▶」で、次の8項目を前後に切り替えられます（矢印ボタンの位置は
表示テキストの長さが変わっても動きません）。

| 表示 | 内容 |
| --- | --- |
| 効果音のみ | なぞり音・成功音などの効果音のみ。BGMは流れない |
| BGMランダム | BGM＋効果音。プレイ開始のたびに5曲からランダムに1曲再生 |
| BGM1〜BGM5 | BGM＋効果音。対応する1曲を固定で再生 |
| 音なし | すべての音を鳴らさない |

起動するたびに（再読み込み・再起動を含む）必ず「効果音のみ」から始まります。切り替えた設定は
その場では`localStorage`に保存されますが、次回起動時には引き継がれず「効果音のみ」にリセットされます。

### BGM

`assets/bgm/`にある5曲を、選んだサウンドモードに応じてランダムまたは固定で再生します。曲ごとに
長さが微妙に異なるため、曲の盛り上がりとゲーム進行が噛み合うよう、曲ごとに再生開始タイミングを
変えています。

| 曲 | 再生開始タイミング |
| --- | --- |
| BGM01_焦りは禁物 | カウントダウン「3」表示と同時 |
| BGM02_てんやわんやなお嬢様 | カウントダウン「2」表示と同時 |
| BGM03_和風ロックBGM | カウントダウン「1」表示と同時 |
| BGM04_ColdHeart | 「START!」表示と同時 |
| BGM05_達成！ | ゲーム開始（START!）から3秒後（残り57秒） |

BGMはタイムアップ時、および「もどる」でタイトルに戻ったときに停止します。

## ローカルでの動作確認

このプロジェクトはビルド不要の静的HTML/CSS/JavaScriptですが、`js/*.js` はES Modules（`import`/`export`）を使っているため、
`index.html` を `file://` で直接開くとブラウザのCORS制限でモジュール読み込みに失敗します。必ず簡易HTTPサーバー経由で開いてください。

```bash
# Node.jsがある場合
npx serve nazoten

# Pythonがある場合
cd nazoten
python -m http.server 8080
```

起動後、表示されたURL（例: http://localhost:8080）をブラウザで開いてください。

GitHub Pagesなど静的Webホスティングにそのまま配置しても動作します。

## ファイル構成

```text
nazoten/
├─ index.html
├─ README.md
├─ css/
│  ├─ base.css        画面共通のレイアウト・ボタン等
│  ├─ game.css         各画面・盤面の見た目、フィーバー配色
│  ├─ battle.css        CPUバトル画面・ゲージ・2盤面レイアウト
│  ├─ two-player.css    2人バトルの上下対面レイアウト・P2の180度回転・結果画面
│  ├─ mixed-battle.css  対戦形式選択・共有盤面の相手選択表示
│  ├─ ojama.css         オジャマ（ターン・スモール・スマイル・メテオ・ハイド）の見た目
│  ├─ animations.css   演出用アニメーション
│  ├─ help.css          あそびかた説明画面（サブメニュー・ページ送り・説明用ミニ盤面）
│  ├─ puzzle.css        じっくり・ステージ選択・スター・可変サイズ盤面・
│  │                      ランダム生成問題（カード・生成中画面・問題ID）
│  ├─ score-attack-select.css  スコアアタックのモード選択画面（おてがる／スタンダード）
│  └─ easy-score-attack.css    おてがるの2×3盤面・お題カード・0.3秒切り替え
├─ js/
│  ├─ config.js         設定値の一元管理（時間・倍率・称号・CPUレベル・オジャマ・じっくり・
│  │                      おてがるの10パターン・両モード設定・保存キー）
│  ├─ main.js           初期化・モジュール接続
│  ├─ help-content.js   あそびかた説明画面の文言・ページ順（基本ルール/スコアバトル/
│  │                      ごちゃまぜバトル/じっくりルールの全24ページ）
│  ├─ help.js           あそびかた説明画面のページ表示・ページ送り・説明用ミニ盤面の描画
│  │                      （本番のBoard/InputController等は生成しない）
│  ├─ game.js           スタンダードスコアアタックの状態遷移・タイマー・得点確定
│  ├─ easy-score-attack.js  おてがるスコアアタックの進行管理（出題・0.3秒切り替え・
│  │                      入れかえ・パス・タイマー・記録）
│  ├─ easy-questions.js おてがるの出題ロジック（2×3経路列挙・数字生成・10問ブロック・検証）
│  ├─ battle.js          スコアバトルCPU戦の進行管理（共通タイマー・両者の得点・ゲージ・勝敗・オジャマ接続）
│  ├─ cpu.js             CPUの経路探索・候補選択・なぞり演出・停止管理・オジャマ被弾時の減速
│  ├─ mixed-cpu-battle.js ごちゃまぜバトルCPU戦の進行管理（1つの共有盤面をプレイヤー入力・CPUの
│  │                      両方が参照・先着判定・相手選択保護・オジャマ接続）
│  ├─ two-player.js      スコアバトルの進行管理（共通タイマー・P1/P2独立入力・得点・ゲージ・勝敗・オジャマ接続）
│  ├─ mixed-battle.js    ごちゃまぜバトルの進行管理（1つの共有盤面・先着判定・相手選択保護・オジャマ接続）
│  ├─ ojama.js           オジャマの自動発動・抽選・解除（CPUバトル/スコアバトル/ごちゃまぜ共通）
│  ├─ board.js           盤面生成・消去・補充（複数盤面インスタンス・共有盤面のどちらにも対応）
│  ├─ input.js           Pointer Eventsとなぞり判定（盤面ごとに独立したインスタンス）
│  ├─ scoring.js         得点計算（合計×マス数×フィーバー倍率＋フォーティボーナス）・
│  │                      シルバー・フィーバー判定・記録
│  ├─ puzzle.js          じっくりの状態・進行・履歴（手数・入れかえ・ヒント・クリア判定、
│  │                      固定ステージ・ランダム生成問題の両方を同じ進行で扱う）
│  ├─ puzzle-stages.js   じっくり全24ステージの固定データ（盤面・お題・公式解法）
│  ├─ puzzle-solver.js   じっくりの純粋ロジック（経路列挙・お題判定・解法探索・ステージ検証）
│  ├─ puzzle-generator.js  じっくりランダム生成問題のseed付き決定的生成ロジック
│  │                      （エリア別4種類の逆算生成・生成後検証・フォールバックseed）
│  ├─ random-puzzle.js   ランダム生成問題の解放判定・生成要求・記録更新
│  │                      （実際のプレイ進行はpuzzle.jsのPuzzleControllerへ委譲）
│  ├─ audio.js           Web Audio合成音（フィーバー・CPU用SE・オジャマ用SE・じっくり用SE含む）
│  ├─ storage.js         ベストスコア・設定・各対戦記録・じっくり進捗（ランダム記録含む）の
│  │                      保存、v1〜v7移行
│  └─ ui.js               画面切替・表示更新・フィーバー演出・バトル画面表示
├─ tests/
│  ├─ puzzle-validation.html  じっくり全24ステージの検証結果を一覧表示する開発用画面
│  └─ puzzle-validation.js    上記が呼び出す検証ロジック（puzzle-solver.jsのvalidateStage等）
└─ assets/
   ├─ icons/
   ├─ images/
   └─ bgm/               BGM音源（mp3、5曲＋対戦結果ジングル2曲）
```

## 実装範囲（Phase 1〜6）

- タイトル → カウントダウン → 60秒プレイ → 結果画面の一連の流れ（あそびかたはタイトルの
  「遊び方」からいつでも確認できる、説明専用の画面）。「遊び方」を押すとサブメニューが開き、
  「基本ルール」「スコアバトル」「ごちゃまぜバトル」「KEC数学科ラボへ」「タイトルへ戻る」から、
  画面イメージ付きの詳しい説明を確認できる（基本ルール7ページ、スコアバトル6ページ、
  ごちゃまぜバトル7ページ。前へ／次へ・ページ番号・ページドットで送る）
- 縦4×横4盤面（同じ数字は最大5個まで）、隣接マスのみをなぞれる操作
- 合計10・20・30・40の判定、フォーティボーナス（40は+100点、フィーバー倍率の影響を受けない）
- 消去後1秒での補充、補充待ち中も他マスの操作を継続可能
- 数字のダブルタップによる得点なし破壊（1秒後に補充、専用の破壊音）
- 数字入れかえ（タップで選択→別の数字をタップして位置を交換。長押し・なぞり操作で選択解除）
- 60秒制の1人用スコアアタック
- ラスト10秒のミリオン・フィーバーと得点3倍（自動発動・1プレイ1回）
- 5マスで合計10を作ると発動する「シルバー・フィーバー」（10秒間得点2倍、ミリオン・フィーバー中は
  発動せず、同時条件ではミリオン・フィーバーを優先）
- フィーバー専用の画面演出（ミリオン・フィーバー＝金色盤面、シルバー・フィーバー＝銀色盤面、
  開始オーバーレイ）・効果音・カウントダウン
- 結果画面のプレイ記録（通常タイムスコア・Mフィーバースコア・成功率・パネルを消した回数・
  10〜40を作った回数・Sフィーバー突入回数・入れ替え/破壊回数・消したパネルの数）
- なぞり音・成功音・40音・フィーバー音・失敗音・カウントダウン音・タイムアップ音・結果音（Web Audio合成音）
- プレイ開始ごとにランダムな1曲を、曲ごとに指定したタイミングで再生するBGM（5曲）
- 「効果音のみ／BGMランダム／BGM1〜5／音なし」の8状態サウンド切り替え（◀▶で選択、起動時は毎回効果音のみから開始）
- 称号表示（LV.1〜LV.20は固有称号、LV.21以降は「超速ナゾテン王+N」として上限なく継続）
- 1P対CPUの60秒スコアアタックバトル
- 強さ1〜5・MAXの6段階CPU（思考速度・探索できる経路の長さ・最善手選択率・ミス率・手詰まり時の破壊が段階ごとに変化）。
  強さはボタンではなくスライドバー（左端が強さ1、右端がMAX）で選択する
- 同じ初期配列から開始し、その後は独立して変化する2盤面（プレイヤー用・CPU用）
- CPUによる有効経路探索、なぞり演出、わざとのミス、得点なしの破壊
- プレイ中の得点を隠して優劣だけを示すバトルゲージ（点差ベースの正規化表示）
- ラスト10秒のゲージ非表示と、両者同時のミリオン・フィーバー
- プレイヤー・CPUそれぞれ独立して発動する「シルバー・フィーバー」（発動側の盤面だけ銀色・得点2倍）
- 勝敗・両者スコア・点差・LV称号の結果発表
- 結果画面では1PとCPUのプレイ記録を横並びの表で比較表示
- CPUレベル別の対戦成績を保存（内部的には勝敗数・自己最高得点・最高点差を記録するが、
  CPU強さ選択画面・結果画面での表示は勝利数のみ）
- タイトルの「遊び方」→「スコアバトル」「ごちゃまぜバトル」からいつでも確認できる、
  CPU戦・2人プレイ戦共通のルール説明（画面イメージ付き）
- 30秒版（Phase 1）・60秒＋フィーバー版（Phase 2）・CPUバトル版（Phase 3）・2人バトル版（Phase 4）を
  分けたベストスコア／対戦記録の保存（`localStorage` v1→v2→v3→v4移行、旧バージョンのデータは削除しない）
- 更新版チュートリアルのバージョン管理（`tutorialVersion`）
- 1台2人用の60秒スコアバトル
- 上下分割レイアウトと2P側の180度回転（盤面のセル配置自体はP1・P2で共通、CSSの回転だけで対応）
- Pointer Eventsによる2人同時操作（盤面ごとに独立した入力コントローラー）
- P1・P2で完全に独立したなぞり・数字入れかえ・ダブルタップ破壊
- 同じ初期盤面（4×4）から開始し、その後は独立して変化する2盤面
- プレイヤーごとに独立したシルバー・フィーバーと、再発動時の残り10秒への更新
- 両者同時のミリオン・フィーバーと、その開始時のシルバー・フィーバー強制終了
- プレイ中の累計得点非表示と、点差ベースの優劣ゲージ
- P1・P2それぞれの向きから確認できる勝敗・スコア・称号・プレイ記録比較の結果画面
  （記録一覧表は内部スクロールし、スマホ縦画面の1画面に収まる）
- 2人バトルの対戦記録の保存（プレイ回数・P1/P2勝利数・引分・自己ベスト・合計最高得点）
- 2人バトル内でのスコアバトル／ごちゃまぜバトルの形式選択画面
- 1つの4×4盤面をP1・P2で共有するごちゃまぜバトル（盤面データは共通、表示はP1・P2それぞれの
  向きに合わせた2つのDOMで描画）
- ごちゃまぜバトルにおける、相手がなぞっている経路のリアルタイム表示（色つきリングで重ね表示、
  同じマスを両者が選択している場合は両者分のリングを表示）
- 同じマス・経路を両者が同時に選択した場合、先に確定した側が得点・消去を獲得する先着処理
- 相手に先に消された経路を確定しようとした場合、失敗扱いにせずペナルティなしで無効化
  (「横取りされた」表示、失敗音は鳴らさない)
- 相手が選択中（なぞり中・入れかえの1つ目選択中）のマスへの入れかえ・破壊の禁止
- 共有盤面の消去・補充・交換・破壊をP1・P2の両方の表示へ同期
- ごちゃまぜバトルの結果画面では称号・LVを表示しない（通常のバトル系結果画面との違い）
- 対戦開始前に選べるオジャマON/OFF（デフォルトON、ページ再読み込みでONへ戻る、保存はしない）
- 経過20秒・40秒（＝残り40秒・20秒）時点での自動発動（ボタン操作なし、同点の場合は発動なし）。
  その時点で勝っている側が自動的に対象になる
- 対象になると7秒間、相手表示への視覚的オジャマが発生する
- 5種類のオジャマ効果「ターン」（数字180度回転）・「スモール」（数字をランダムに縮小）・
  「スマイル」（盤面上を動く絵文字で一部を隠す）・「メテオ」（右上から左下へ降り注ぐ⭐で
  一部を隠す）・「ハイド」（数式表示を黒い矩形で完全に隠す）。盤面データ・当たり判定・得点には
  一切影響しない
- オジャマを受けた側の残り時間表示の下に、受けた種類（「オジャマ「メテオ」」など）を7秒間表示
- ミリオン・フィーバー開始時のオジャマの強制終了（両者の演出が重ならないようにする）
- ごちゃまぜバトルの対戦記録の保存と、`localStorage` v4→v5への移行（旧バージョンのデータは削除しない）
- CPUバトルにも対戦形式選択（スコアバトル／ごちゃまぜバトル）とオジャマON/OFFの選択画面を追加
- 1つの共有盤面をプレイヤーとCPUが早い者勝ちで奪い合う「ごちゃまぜバトルCPU戦」
- ごちゃまぜバトルCPU戦でも、CPUが現在なぞっている経路の保護（プレイヤーは入れかえ・破壊できない）、
  先に取られた場合のペナルティなし解除、称号・LV非表示の結果画面をそのまま踏襲
- CPUバトル（スコアバトル・ごちゃまぜバトルの両方）でのオジャマ対応：CPUが劣勢時に自動でオジャマを
  使用し、プレイヤーがCPUへのオジャマに成功すると見た目の種類にかかわらず5秒間CPUの思考時間・
  なぞり操作時間が2倍になる
- ごちゃまぜバトルCPU戦の対戦記録をCPUレベル別に保存し、`localStorage` v5→v6へ移行
  （旧バージョンのデータは削除しない）
- サウンドモードがBGM系（BGMランダム／BGM1〜5）のとき、CPU戦・2人対戦の結果画面表示と同時に
  合成SEの代わりにWIN/LOSEジングル（mp3、ループなし）を再生（「効果音のみ」では従来の合成SEを維持）
- BGM03・BGM04の再生開始タイミングを1秒早め、BGM05を「START!」と同時のタイミングに変更
- 1人用ステージパズル「じっくり」モード
- 4エリア×6問、全24ステージ（3×3・4×4・5×5の固定盤面）
- 指定合計、指定マス数、順番、全消去の4種類のお題と、ステージごとの制限手数
- 全ステージで数字を補充せず、ダブルタップ破壊も使用しない
- 専用ステージだけで使える、手数を消費する数字入れかえ
- 「1手戻す」「ヒント」「やり直す」（ヒントは公式解法の接頭辞一致を優先し、外れた場合のみ
  残り手数以内の解法を反復深化探索。探索上限到達時は手詰まり案内を表示）
- クリア・目標手数以内・ノーヒントの3条件による最大3スター評価とステージ別ベスト記録の保存
- ステージの順次解放（クリアで次のステージ、エリア最終問クリアで次エリアの1問目を解放）と
  「つづきから」（最後に開いたステージ→未クリアの最小番号→全クリア時は最終ステージの優先順）
- じっくり専用の初回説明（あそびかたの「じっくりルール」を再利用し、初回のステージ開始前にだけ
  自動表示）と、入れかえ専用ステージ向けの一度きりの操作説明
- じっくりモードはBGMを鳴らさず、タイトルで選ばれているサウンドモードが「音なし」以外なら
  効果音のみ再生（1手戻す・ヒント・ステージクリア・エリアクリア・全クリア専用の効果音を含む）
- ステージデータの自動検証（`tests/puzzle-validation.html`で全24ステージの公式解法・手数・
  入れかえ回数・セル値などを検証し「24 / 24 PASS」を確認）
- `localStorage`のじっくり進捗保存（v6形状の一部として追加。既存のベストスコア・対戦記録には
  影響しない）
- タイトルの「スコアアタック」からモード選択画面（おてがる／スタンダード）を挟む構成に変更し、
  タイトル画面のハイスコア表示を削除（選択画面で両モードのBESTを個別に表示）
- 2×3盤面・10種類の出題パターンによる1問形式の「おてがるスコアアタック」を追加
  （既存の4×4モードは名称を「スタンダード」に変更。内部進行・保存キーは変更しない）
- 10問単位のブロックで出題順を制御（1・2・6・10問目は固定パターン、残り6問はシャッフル）
- 初期化時に2×3盤面の全経路を列挙し、指定合計・指定マス数を満たす経路を必ず1つ以上含む
  盤面を生成（生成上限到達時は検証済みフォールバック盤面を使用）
- 正解・パス（ダブルタップ）から0.3秒後に6マスすべてを次の問題へ一括更新
  （縮小→拡大のパネル切り替え演出付き）
- おてがるでも数字入れかえを回数無制限で使用可能。シルバー・ミリオン・フィーバーは不使用
- ミスなし・入れかえなしで正解した問題1問につき+20点のボーナス
- おてがる結果画面のLV・称号表示（正解数＋√連続正解数の専用計算式）
- おてがる専用の初回自動説明（あそびかたの「おてがるスコアアタック」を再利用）と、
  おてがる専用のベストスコア・プレイ記録の保存（スタンダードとは別枠、途中退出時は更新しない）
- じっくりモードの各エリアStage 6クリアで解放される「ランダム問題」を追加
  （正解手順から盤面を逆算して生成し、公式解法・ソルバー検証に合格した問題だけを出題）
- ランダム問題はseed・生成バージョンから決定的に再現でき、問題IDで表示・確認できる
- エリア1は指定合計、エリア2は指定マス数・順番、エリア3は全消去、エリア4は
  4×4固定・入れかえ必須の問題をそれぞれ生成（5×5は生成しない）
- ランダム問題専用の記録（プレイ・クリア・ノーヒント・パーフェクト・連続クリア数）を
  エリア別に保存し、固定24ステージのスター・進捗には一切影響しない
- 通常生成に失敗した場合の検証済みフォールバックseedと、生成失敗時のエラー表示
- `localStorage` v6→v7移行（既存のベストスコア・対戦記録・固定ステージ進捗を維持）
- CPUバトル（スコアバトル・ごちゃまぜバトルの両方）に、強さ1～5・MAXの6段階へ
  個性を持たせる敵キャラクター演出を追加
- CPU強さ選択画面のキャラクターカード（絵文字・名前・フレーバーテキスト・CPU性能説明・
  選択中の対戦形式に対応する通算勝利数、スライダー操作への即時追従）
- CPU戦プレイ画面のCPU盤面右側キャラクターHUD（優劣ゲージのCPU側比率から5段階で
  絵文字サイズを変化させ、ミリオン・フィーバー中は等倍固定）
- CPU視点の勝敗・点差を7区分に分類し、キャラクターごとの結果セリフを吹き出しで表示
- キャラクター演出はCPUの思考パラメータ・得点・戦績・既存の保存データに一切影響しない

オンライン対戦など将来拡張機能は未実装です。
